
let input =10;
let array = []
for(let i=2;i<= input;i++){
    let count = 0;
    for(let j=2;j<i;j++){
        if(i%j == 0){
            count++;
         }
    }if(count === 0){
                array.push(i);
            }
}
const result = array.reduce((b,a) => a+b, 0)
console.log(result,array)
